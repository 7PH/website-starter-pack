# ⚠️ STARTERPACK CORE — DO NOT MODIFY. This file is managed by the starterpack.
FROM node:20-alpine3.20 AS build-stage

# Must match Dockerfile.prod-node, or the two builds' _nuxt hashes diverge and every /_nuxt/*.js 404s.
ARG SITEMAP_ENABLED=true
ENV SITEMAP_ENABLED=${SITEMAP_ENABLED}

WORKDIR /app

COPY . .

RUN npm install
RUN npm run build

FROM nginx:1.26-alpine3.20-perl AS running-stage

WORKDIR /usr/share/nginx/html

COPY ./nginx.conf /etc/nginx/nginx.conf
COPY ./nginx.default.conf /etc/nginx/conf.d/default.conf
COPY --from=build-stage /app/.output/public /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
